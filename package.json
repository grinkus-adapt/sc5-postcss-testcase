{
  "name": "sc5-postcss-testcase",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build-css.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm run build && styleguide --parsers \"{ css: 'postcss' }\" --kss-source \"src/**/*.css\" --style-source \"dist/**/*.css\" --output styleguide --watch --server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postcss": "5.0.12",
    "postcss-nested": "1.0.0",
    "sc5-styleguide": "0.3.41"
  }
}
